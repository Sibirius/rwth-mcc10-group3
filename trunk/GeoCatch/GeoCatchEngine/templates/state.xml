<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<state 
	mode ="{{mode}}"
	state="{{state}}"
	
	{% if mode == 0 %} {% comment %} race TODO {% endcomment %}
	{% endif %}	
			
	{% if mode == 1 %} {% comment %} chaincatch {% endcomment %}
		lat="{{modespecific.lat}}"
		lon="{{modespecific.lon}}"
	{% endif %}
/>

<events>
	{% for event in events %}
	<event 
		title="{{event.title}}"
		info="{{event.info}}"
	/>		
	{% endfor %}
</events>
